/**
 * Paystack redirects here after Ladipo checkout. UI and verification are handled by
 * {@link LadipoPaystackReturnHandler} in App.jsx (processing/success modals, no standalone page).
 */
export default function LadipoPaymentCallback() {
  return null;
}
